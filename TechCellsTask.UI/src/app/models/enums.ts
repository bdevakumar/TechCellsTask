export enum RequestObserve {
    Response = "response",
    Events = "events",
    Body = "body",
}

export enum ResponseType {
    ArrayBuffer = "arraybuffer",
    Blob = "blob",
    Text = "text",
    Json = "json"
}